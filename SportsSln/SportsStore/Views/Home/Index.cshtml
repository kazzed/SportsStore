@model IQueryable<Product>

<h2>Welcome to SportsStore</h2>

@foreach (var product in Model ?? Enumerable.Empty<Product>()) {
    <div>
        <h3>@product.Name</h3>
        @product.Description
        <h4>@product.Price.ToString("c")</h4>
    </div>
}
